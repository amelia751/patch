// Vendored dependency. It names imagen-4.0-generate-001, but it is not the
// customer's code, so the scanner must not descend into `vendor/` and this
// occurrence must be absent from the inventory.
module.exports.MODEL = "imagen-4.0-generate-001";
