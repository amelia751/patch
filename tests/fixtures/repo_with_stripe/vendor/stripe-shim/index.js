// Vendored dependency. It names stripe.handleCardPayment, but it is not the
// customer's code, so the scanner must not descend into `vendor/` and this
// occurrence must be absent from the inventory.
module.exports.legacy = "stripe.handleCardPayment";
